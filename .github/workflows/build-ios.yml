name: Build iOS Unsigned

on:
  workflow_dispatch:
  push:
    branches:
      - main
    paths:
      - '.github/workflows/build-ios.yml'
      - 'ci/v11_6_1/**'
      - 'ci/v11_6_3/**'
      - 'ci/v11_6_5/**'
      - 'ci/v11_6_6/**'
      - 'ci/v11_6_7/**'
      - 'ci/v11_6_8/**'
      - 'ci/v11_6_9/**'
      - 'ci/v11_6_10/**'
      - 'ci/v11_6_11/**'
      - 'ci/v11_6_12/**'
      - 'ci/v11_6_13/**'
      - 'GardeFlow_android_v11_6_0_official_pdf_auto_import.zip'

permissions:
  contents: read

jobs:
  build-ios:
    runs-on: macos-latest
    timeout-minutes: 60

    steps:
      - name: Checkout repository
        uses: actions/checkout@v4

      - name: Set up Flutter
        uses: subosito/flutter-action@v2
        with:
          channel: stable
          cache: true

      - name: Locate GardeFlow project
        id: project
        shell: bash
        run: |
          set -euo pipefail
          ZIP="GardeFlow_android_v11_6_0_official_pdf_auto_import.zip"
          test -f "$ZIP"
          rm -rf .ci-ios-project
          mkdir -p .ci-ios-project
          ditto -x -k "$ZIP" .ci-ios-project
          PUBSPEC="$(find .ci-ios-project -type f -name pubspec.yaml -print -quit)"
          test -n "$PUBSPEC"
          PROJECT_DIR="$(dirname "$PUBSPEC")"
          test -f "$PROJECT_DIR/lib/main.dart"
          echo "dir=$PROJECT_DIR" >> "$GITHUB_OUTPUT"
          echo "Project: $PROJECT_DIR"

      - name: Apply V11.6.1 overlay
        working-directory: ${{ steps.project.outputs.dir }}
        shell: bash
        run: |
          set -euo pipefail
          cat "$GITHUB_WORKSPACE"/ci/v11_6_1/overlay.part* > /tmp/gardeflow_v11_6_1.patch
          patch -p1 < /tmp/gardeflow_v11_6_1.patch
          grep -q '^version: 11.6.1+161$' pubspec.yaml

      - name: Apply V11.6.3 UI overlay
        working-directory: ${{ steps.project.outputs.dir }}
        shell: bash
        run: |
          set -euo pipefail
          cat "$GITHUB_WORKSPACE"/ci/v11_6_3/overlay.part* > /tmp/gardeflow_v11_6_3.patch
          patch -p1 < /tmp/gardeflow_v11_6_3.patch
          grep -q '^version: 11.6.3+163$' pubspec.yaml

      - name: Set V11.6.4 and configure iOS project
        working-directory: ${{ steps.project.outputs.dir }}
        shell: bash
        run: |
          set -euo pipefail
          python3 - <<'PY'
          from pathlib import Path
          p = Path('pubspec.yaml')
          p.write_text(p.read_text().replace('version: 11.6.3+163', 'version: 11.6.4+164'))
          p = Path('tool/configure_ios.dart')
          p.write_text(p.read_text().replace("const deploymentTarget = '13.0';", "const deploymentTarget = '15.0';"))
          PY
          if [ ! -f ios/Runner.xcodeproj/project.pbxproj ]; then
            rm -rf ios
            flutter create --no-pub --platforms=ios --org com.huim6 --project-name huim6_planning .
          fi
          dart tool/configure_ios.dart
          grep -q '^version: 11.6.4+164$' pubspec.yaml

      - name: Fix iOS launcher icon scale
        working-directory: ${{ steps.project.outputs.dir }}
        shell: bash
        run: |
          set -euo pipefail
          python3 -m pip install --quiet --break-system-packages pillow || python3 -m pip install --quiet --user pillow
          python3 "$GITHUB_WORKSPACE/ci/v11_6_6/fix_launcher_icons.py" --ios

      - name: Apply V11.6.5 official roster backfill
        working-directory: ${{ steps.project.outputs.dir }}
        shell: bash
        run: |
          set -euo pipefail
          cat "$GITHUB_WORKSPACE"/ci/v11_6_5/overlay.part* > /tmp/gardeflow_v11_6_5.patch
          patch -p1 < /tmp/gardeflow_v11_6_5.patch
          python3 - <<'PY'
          from pathlib import Path
          p = Path('pubspec.yaml')
          p.write_text(p.read_text().replace('version: 11.6.4+164', 'version: 11.6.5+165'))
          PY
          grep -q '^version: 11.6.5+165$' pubspec.yaml

      - name: Apply V11.6.6 HUICK and astreinte hospitals
        working-directory: ${{ steps.project.outputs.dir }}
        shell: bash
        run: |
          set -euo pipefail
          cat "$GITHUB_WORKSPACE"/ci/v11_6_6/overlay.part* > /tmp/gardeflow_v11_6_6.patch
          patch -p1 < /tmp/gardeflow_v11_6_6.patch
          grep -q '^version: 11.6.6+166$' pubspec.yaml

      - name: Apply V11.6.7 reminders and PDF search
        working-directory: ${{ steps.project.outputs.dir }}
        shell: bash
        run: |
          set -euo pipefail
          python3 "$GITHUB_WORKSPACE/ci/v11_6_7/apply_v11_6_7.py"
          grep -q '^version: 11.6.7+167$' pubspec.yaml

      - name: Apply V11.6.8 PDF viewer hotfix
        working-directory: ${{ steps.project.outputs.dir }}
        shell: bash
        run: |
          set -euo pipefail
          python3 "$GITHUB_WORKSPACE/ci/v11_6_8/apply_v11_6_8.py"
          grep -q '^version: 11.6.8+168$' pubspec.yaml

      - name: Apply V11.6.9 PDF and reminder UI fixes
        working-directory: ${{ steps.project.outputs.dir }}
        shell: bash
        run: |
          set -euo pipefail
          python3 "$GITHUB_WORKSPACE/ci/v11_6_9/apply_v11_6_9.py"
          grep -q '^version: 11.6.9+169$' pubspec.yaml

      - name: Apply V11.6.10 notification startup hotfix
        working-directory: ${{ steps.project.outputs.dir }}
        shell: bash
        run: |
          set -euo pipefail
          python3 "$GITHUB_WORKSPACE/ci/v11_6_10/apply_v11_6_10.py"
          grep -q '^version: 11.6.10+170$' pubspec.yaml
          ! grep -q '_plugin.cancelAll()' lib/services/notification_service.dart

      - name: Apply V11.6.11 functional password recovery
        working-directory: ${{ steps.project.outputs.dir }}
        shell: bash
        run: |
          set -euo pipefail
          python3 "$GITHUB_WORKSPACE/ci/v11_6_11/apply_v11_6_11.py"
          grep -q '^version: 11.6.11+171$' pubspec.yaml
          test -f lib/screens/forgot_password_screen.dart
          grep -q "password-reset" lib/services/supabase_backend_service.dart

      - name: Apply V11.6.12 admin-mediated password reset
        working-directory: ${{ steps.project.outputs.dir }}
        shell: bash
        run: |
          set -euo pipefail
          python3 "$GITHUB_WORKSPACE/ci/v11_6_12/apply_v11_6_12.py"
          grep -q '^version: 11.6.12+172$' pubspec.yaml
          test -f lib/screens/admin_password_reset_screen.dart
          grep -q "requestPasswordResetHelp" lib/services/supabase_backend_service.dart
          grep -q "adminResetUserPassword" lib/services/supabase_backend_service.dart

      - name: Apply V11.6.13 admin account deletion
        working-directory: ${{ steps.project.outputs.dir }}
        shell: bash
        run: |
          set -euo pipefail
          python3 "$GITHUB_WORKSPACE/ci/v11_6_13/apply_v11_6_13.py"
          grep -q '^version: 11.6.13+173$' pubspec.yaml
          grep -q "adminDeleteUserAccount" lib/services/supabase_backend_service.dart
          grep -q "delete_forever_rounded" lib/screens/admin_password_reset_screen.dart

      - name: Install Flutter dependencies
        working-directory: ${{ steps.project.outputs.dir }}
        shell: bash
        run: |
          set -euo pipefail
          flutter clean
          flutter pub get
          if [ -f ios/Podfile ]; then
            python3 - <<'PY'
          from pathlib import Path
          import re
          p = Path('ios/Podfile')
          s = p.read_text()
          if re.search(r'^#?\s*platform :ios,', s, flags=re.M):
              s = re.sub(r'^#?\s*platform :ios,\s*[\'\"][^\'\"]+[\'\"]', "platform :ios, '15.0'", s, count=1, flags=re.M)
          else:
              s = "platform :ios, '15.0'\n" + s
          p.write_text(s)
          PY
            (cd ios && pod install --repo-update)
          fi

      - name: Show Xcode and Flutter versions
        working-directory: ${{ steps.project.outputs.dir }}
        run: |
          xcodebuild -version
          flutter --version
          flutter doctor -v

      - name: Build iOS release without code signing
        working-directory: ${{ steps.project.outputs.dir }}
        shell: bash
        run: |
          set -euo pipefail
          flutter build ios --release --no-codesign
          test -d build/ios/iphoneos/Runner.app

      - name: Package unsigned iOS artifacts
        working-directory: ${{ steps.project.outputs.dir }}
        shell: bash
        run: |
          set -euo pipefail
          rm -rf build/ios/package
          mkdir -p build/ios/package/Payload
          cp -R build/ios/iphoneos/Runner.app build/ios/package/Payload/Runner.app
          ditto -c -k --sequesterRsrc --keepParent build/ios/iphoneos/Runner.app build/ios/GardeFlow-v11.6.13-Runner.app.zip
          (cd build/ios/package && zip -qry ../GardeFlow-v11.6.13-unsigned.ipa Payload)
          ls -lh build/ios/GardeFlow-v11.6.13-Runner.app.zip build/ios/GardeFlow-v11.6.13-unsigned.ipa

      - name: Upload iOS unsigned artifacts
        uses: actions/upload-artifact@v4
        with:
          name: GardeFlow-v11.6.13-ios-unsigned
          path: |
            ${{ steps.project.outputs.dir }}/build/ios/GardeFlow-v11.6.13-Runner.app.zip
            ${{ steps.project.outputs.dir }}/build/ios/GardeFlow-v11.6.13-unsigned.ipa
          if-no-files-found: error
          retention-days: 30
